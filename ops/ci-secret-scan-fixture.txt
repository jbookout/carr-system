This benign, tracked fixture exists only for ops/ci-selftest.py.

The self-test temporarily replaces these bytes under its crash-recovery journal
to prove ci-secret-scan catches a shaped credential and respects its same-line
allow marker. No operational program reads this file.
