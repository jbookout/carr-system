<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Re-applies the Logitech modifier repair (see bin/logitech-keymap.sh).
         hidutil mappings die on reboot and on receiver replug, so this runs at
         login and every 10 minutes; the set is idempotent and costs a few ms. -->
    <key>Label</key>
    <string>com.carr.logitech-keymap</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/zsh</string>
        <string>{{REPO}}/bin/run-scheduled.sh</string>
        <string>logitech-keymap</string>
        <string>launchd.run</string>
        <string>/bin/sh</string>
        <string>{{REPO}}/tools/dictation-rig/bin/logitech-keymap.sh</string>
      </array>
    <key>RunAtLoad</key>
    <true/>
    <!-- carr-launchd-interval-seconds: 600 | rendered by lib/launchd_calendar.py; macOS 27 launchd never fires StartInterval -->
    <key>StartCalendarInterval</key>
    <array>
        <dict><key>Minute</key><integer>0</integer></dict>
        <dict><key>Minute</key><integer>10</integer></dict>
        <dict><key>Minute</key><integer>20</integer></dict>
        <dict><key>Minute</key><integer>30</integer></dict>
        <dict><key>Minute</key><integer>40</integer></dict>
        <dict><key>Minute</key><integer>50</integer></dict>
    </array>
</dict>
</plist>
