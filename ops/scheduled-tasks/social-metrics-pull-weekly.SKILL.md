---
name: social-metrics-pull-weekly
description: Weekly social analytics pull: reads X metrics via the Grok CLI and FB/IG/LinkedIn metrics from Joe's logged-in Chrome, then records them through the measure-placement verb so the monthly review steers from data
---

STORE-FIRST (added 2026-08-09, loop #289): the doctrine STORE is the source of truth for every governing doc named below. Before reading any `.md` path in the vault, try `read-doctrine` with that file's stem as the document slug; if a store doc exists, IT WINS and the vault file may be a stale duplicate. Two such duplicates were found on 2026-08-09 and this routine's sibling had been reading a three-week-old SOP because its pointer named the file instead of the slug. Do not edit the vault copy either way: hand-authored vault markdown is closed by record-home-gate.py (rule 14181e60).

You are running Joe Bookout's weekly social-media metrics pull (CARR AI system). This is a MEASUREMENT-ONLY task: you read analytics and record them through the verbs, plus (since 2026-07-31) you kick off the record layer's own metrics chain with one command. You never post, publish, schedule, or reply to anything.

WHERE THE NUMBERS GO — CORRECTED 2026-08-12, and this supersedes every instruction below that says to fill post-performance-log.md. That file is CLOSED. Attempting to write it returns: "BLOCKED by the CARR record-home gate: hand-authored markdown in the vault is closed (Joe, 2026-08-07: 'i don't want anything written into .md files... database first' — rule 14181e60)." Every measurement goes through the **`measure-placement`** verb instead, one call per post, each with a fresh UUID idempotency_key. That verb is the right home and it is better than the file was: it stores metric kinds verbatim as each platform names them, it records an ATTEMPTED-but-empty read as a first-class `unavailable:true` row so an unmeasured post can never silently read as a zero, and the weekly learning job counts off it directly. Put the per-post Learning line in the verb's `note` field, which is where the comparative takeaway now lives. Do NOT try to route around the gate, and do NOT hand Joe the blocked write to paste.

TWO THINGS THAT MUST EXIST BEFORE `measure-placement` WILL RESOLVE A POST. First, the placement row: placements are created by STEP 0's pull when a post publishes, so if the verb answers `placement_not_found` for a live post, STEP 0 has not applied since that post went out — check that its report header reads APPLY and not DRY RUN before assuming the post is the problem. Second, the post must have gone out through Blotato: anything Joe published by hand on the platform itself has no placement and never will, and the verb correctly refuses to invent one. Report those posts in your summary as measured-but-unrecordable rather than forcing them anywhere.

STEP 0 — THE RECORD-LAYER CHAIN, FIRST, BEFORE ANY BROWSER WORK (added 2026-07-31, ORDER 15). Execute EXACTLY this via Bash, VERBATIM, character for character — do not paraphrase it, do not add flags, do not substitute paths, do not re-quote it. Permission approval matches the exact command string, and any rewording can hit a permission prompt in an unattended run with nobody there to answer it. That is not hypothetical: on 2026-07-31 a scheduled run sat 5 hours 58 minutes on exactly that prompt and produced nothing.

cd ~/carr-system && ./bin/learning-weekly.sh; echo "learning-weekly exit=$?"

Do NOT invoke it as `bash ./bin/learning-weekly.sh`: the script is `#!/bin/zsh` and its logging helper uses zsh's `print` builtin, so under bash every timestamped log line dies with `print: command not found` and a broken step leaves no FAIL line to find.

That one command does three things: pulls Joe's published posts and their metrics from the Blotato API into the record layer (`content_piece` / `placement` / `placement_metric`), then runs the weekly learning job, then runs the correction miner. It writes its reports to CARR AI/Automation/Learning/ and logs to ~/carr-system/out/learning.log.

THEN VERIFY BY OUTPUT, NOT BY THE EXIT CODE (protocol rule 28 — an automation is verified by what it produced, never by its schedule existing and never by its own claim of success). Read the first bold line of each of these three files and quote them in your final summary:
- CARR AI/Automation/Learning/placement-pull-latest.md
- CARR AI/Automation/Learning/weekly-learning-latest.md
- CARR AI/Automation/Learning/correction-miner-latest.md

A report saying "no conclusions yet, threshold is 30" or "the placement records could not be read under this credential" is a SUCCESSFUL run — those jobs are built to speak honestly below their evidence floor, and a below-threshold report is the correct output, not a fault. The only real failures are a FAIL line in out/learning.log or a report file that did not update. Do NOT edit any file under Automation/Learning/ — they are generated. Do NOT try to fix a credential yourself; if a step reports one missing, say which one in your summary and move on to the browser work below.

CONTEXT TO READ FIRST (all under {{VAULT}}/):
1. Marketing/Social Media/post-performance-log.md — READ-ONLY now, and it stops at 2026-08-07. It is the historical record through that date and the place to read prior Learnings for comparison, which is what makes a new Learning comparative. Do not write to it (see the corrected section above); everything from 2026-08-12 forward lives in the record layer. Its "How to use" header still describes the platform routes usefully, but its route (2) "Claude in Chrome for all four platforms" is superseded for X by the Grok-first rule below.
2. Marketing/Social Media/published-log.md — background on what's published and each post's live URL, for the CARR-branded platforms (FB/IG/LinkedIn) and for content/topic labels. For X specifically, treat this file as INCOMPLETE by default (see step 2 below) — it has a history of drifting out of sync with what's actually live.
3. Marketing/Social Media/x-posting-spec.md — X runs as a separate lane from the CARR-branded batch (own cadence, standalone posts, own topics). Don't assume an X post exists only because a same-day FB/LinkedIn/IG post does, or vice versa.

THE JOB:
1. Identify published posts that are ~5+ days old and carry no metrics yet. The authoritative "already measured?" check is the record layer, not a file: read the feature-cell table in Automation/Learning/weekly-learning-latest.md (regenerated by STEP 0) for measured-vs-tagged counts per platform, and treat any placement without metrics as due. post-performance-log.md covers posts through 2026-08-07 only.
   - **Facebook/Instagram/LinkedIn:** cross-reference published-log.md for live URLs and topic/pillar labels; it is reliable for these three.
   - **X:** do NOT rely on published-log.md's post list alone, it has repeatedly been found incomplete (a 2026-07-22 audit found a 1.2K-impression post and 3 others live with no record; a 2026-08-12 audit found the 8/07 county-growth post the same way). Enumerate what is actually live instead: ask Grok for the account's recent posts, or read the @joebookout profile timeline back past the window. Treat THAT as the source of truth for "what's live," and use published-log.md only for topic/pillar labels. Skip quote-tweets, which belong to the daily reply run (rule `25fab860`).
2. Use the claude-in-chrome tools (load via ToolSearch if deferred; call tabs_context_mcp first, create a tab) to read each post's analytics from Joe's logged-in Chrome:
   - Facebook + Instagram: Meta Business Suite (business.facebook.com) — Views/Reach + engagements per post. IG reels: views/reach/likes/comments/shares.
   - LinkedIn: open each post's analytics (impressions, profile views/followers gained if shown).
   - **X: GROK IS THE FIRST MOVE, NOT CHROME.** This is rule `57d13061`, and a 2026-08-12 run violated it after reciting it, so it is spelled out here. Run the Grok CLI headless from the repo and hand it the post IDs:

     `cd ~/carr-system && grok -p '<prompt naming the post IDs and the exact output columns>'`

     Ask for one pipe-delimited line per post: POST_ID, DATE, VIEWS, LIKES, REPLIES, REPOSTS, BOOKMARKS, first ~50 chars of text. Grok's native X tools returned all 25 requested posts in a single call in about a minute on 2026-08-12. It needs no logged-in session, and it distinguishes a true **0** from "no figure displayed" — which the browser cannot, because X renders a bare icon with no number and a Chrome read has to record those as n/c. The same 25 posts cost a Chrome subagent 207 tool calls and about 25 minutes for a strictly worse result. Never start with WebFetch, which always fails on X.

     **GROK IS SUFFICIENT FOR THIS TASK — you do not need the Analytics dashboard at all.** Verified head-to-head on 2026-08-12 across 15+ posts with zero discrepancies: **X's public view counter and Creator Analytics Impressions are the same number.** Matched pairs included 29/29, 25/25, 18/18, 30/30, 38/38 and the 8/01 outlier at 8 likes on both. Grok is in fact MORE precise at the high end, returning the exact 1172 where the dashboard rounds to "1.2K". So numbers pulled through Grok are directly comparable to every pre-2026-08-12 row in post-performance-log.md, which used Analytics Impressions. Do not caveat them as a different series; they are not.

     **The Chrome exception lane still exists, but it is narrower than the Grok-first rule's wording suggests.** That rule lists "Creator Analytics and impressions dashboards" among the surfaces only Chrome reaches, and for PER-POST IMPRESSIONS that is no longer true. Chrome remains required for the rest of that list — X Radar and Premium trend volumes, DMs, bookmarks, protected accounts — and for the Analytics Overview/Audience tabs, which Grok cannot enumerate. Reach for it there, not here.

     **If you do open the dashboard, check the account first.** It lives at `x.com/i/account_analytics/content?type=posts&sort=date&dir=desc&days=28`. On 2026-08-12 it returned the "Advanced analytics with X Premium — Upgrade" paywall, which looks like a lapsed subscription and was not: the Chrome profile was signed in as **@JoeyTrenches** instead of @joebookout. Joe's Premium+ is and was active. Both accounts are already in that profile's switcher, so the fix is one click on the account menu at the bottom of the left rail and needs no credentials. The fastest tell that Chrome is on the wrong account, quicker than loading analytics: a **Follow** button appears beside Joe Bookout in the "Relevant people" panel on his own post, because an account cannot follow itself. Never click Upgrade.

     Do NOT use the per-tweet "Post Analytics" popup (the bar-chart icon under a tweet, or a post's `/analytics` URL) for Impressions: that figure is broken and can read 0 on posts with real likes and replies, confirmed wrong on 8 posts on 2026-07-22.
   If Chrome is not connected or a platform is logged out, SKIP that platform gracefully and note it — never try to log in, never handle credentials.
3. Record each post with one `measure-placement` call: the live post URL as `placement`, `source: platform_native` (never `blotato_api`, which the verb refuses), the real read time as `observed_at`, and the metrics object keyed by each platform's OWN words snake_cased — do not map one platform's word onto another's. X: views_count, likes_count, replies_count, reposts_count, bookmarks_count. Instagram: views_count, accounts_reached_count, likes_count, comments_count, shares_count, saves_count. LinkedIn: impressions_count, reactions_count, comments_count, reposts_count, profile_viewers_count. Facebook's content_library exposes no separate reactions or shares column, so record its combined figure as engagement_count and SAY in the note that it is not a like count. Write the one-line Learning into the `note` field — that line is the whole point of the run. Keep it comparative and concrete ("reel 6x the statics around it", "the story version of the same subject did 73x the one-liner").
4. If step 1 turns up an X post that is live but missing from published-log.md, do NOT edit published-log.md — the record-home gate closes it too. Measure the post like any other and say in the `measure-placement` note that it was FOUND BY AUDIT and absent from the published log, then name it in your final summary so the gap is visible. (A 2026-08-12 audit of the live timeline caught one this way: the 8/07 Walton-vs-Escambia county post, live and unlogged.) Describe the post plainly from what you read; do not guess at intent you cannot see in the text. If the drift keeps recurring, that is a finding about published-log.md's upkeep, not a file to patch by hand.

   QUOTE TWEETS ARE OUT OF SCOPE, always (rule `25fab860`): they belong to the daily X reply run, never to the weekly batch. Skip them when enumerating, and do not measure them here.
5. If a full batch is now measured, file the batch-level signal — the cross-post pattern no single `measure-placement` note can carry — with `record-finding`, using subject_kind `platform` or `format` and the matching marketing_subject slug (that is what those kinds exist for). One finding per real pattern, not one per post. This replaces the old "append a Batch N readout section" instruction, which wrote to a file that is now closed. Follow DNA/writing-rules.md style rules in anything you write (no em-dashes etc.).
6. Data quality: never invent a number. If an analytics page won't load or a number is ambiguous, leave the cell as "n/c" with a note. Numbers you didn't read do not go in the log.

GUARDRAILS: read-only in the browser (navigate + read; no clicking Post/Boost/Promote/Upgrade/any action button). Do not touch Blotato BY HAND — no posting, scheduling, editing or cancelling, in the app or through its API. (Amended 2026-07-31: step 0's script READS the Blotato API for published posts and their analytics. That read is the exception and it is the only one; everything else about Blotato stays hands-off.) Write NO vault markdown at all: post-performance-log.md is closed by the record-home gate and the metrics go through `measure-placement` (see the corrected section near the top). Never hand-edit anything under Automation/Learning/, which is generated by step 0's script. Weekends-off rule does not apply (this is a Wednesday task) but if the run lands on a holiday or Chrome is unavailable, just skip and note it — Monday's brief absorbs gaps.

OUTPUT: end with a short summary — how many rows filled, per-platform highlights (best/worst), anything skipped and why, and one sentence on what the data suggests for the next batch (informational only; the monthly review makes the actual steering decision).