{
  "_comment": "credential-inventory.v1.json — data for ops/credential-health.py (the daily credential-health lane wired into tools/health-check.py). One entry per credential CARR needs to run unattended. Adding a credential later is a config edit here, not a code change. NEVER put a secret VALUE in this file — same rule secrets-inventory.md states for itself. Built 2026-09-24 off Joe's survey: CARR is moving off interactive logins to long-lived scoped tokens, and this lane is what tells him before one lapses.",
  "_probe_types": "shell_exit_status (run `command`, exit code only, stdout/stderr discarded) | cloudflare_auth (CLOUDFLARE_API_TOKEN env if set -> Cloudflare token-verify endpoint, HTTP status + expires_on field only; else falls back to shell_exit_status's `command` — retained for a machine that has not migrated off `wrangler login` yet) | cloudflare_token_file (reads token_key out of the dotenv-shaped file at `path`, mode-600-checked, never the wrangler fallback -> Cloudflare token-verify endpoint, HTTP status + result.status + result.expires_on only) | claude_cli_token_age (reads token_key out of the dotenv-shaped file at `path`, checks length/prefix shape, runs `claude -p \"Reply with exactly: PONG\" --max-turns 1` from a throwaway cwd with ONLY that one env var set and scans only for the word PONG, and ages the token from a first-seen mint date this lane records itself in out/credential-health-mint-state.json — never the token value) | neon_api (reads token_key out of the dotenv-shaped file at `path` if configured and present, else NEON_API_KEY env -> Neon API, HTTP status only) | worker_bearer_health (reads token_key out of the dotenv-shaped file at `path` if configured and present, else the token_env env var -> POST tools/list against the deployed Worker's /mcp, HTTP status only) | file_presence_age (local file's existence + mtime age, never its contents) | config_only (no live probe; status comes entirely from the `expiry` block below, which Joe edits by hand)",
  "credentials": [
    {
      "name": "cloudflare-deploy-token",
      "display_name": "Cloudflare API token (Worker deploys)",
      "kind": "api_key",
      "machines": ["studio"],
      "location": "~/.config/carr/tokens.env (key CLOUDFLARE_API_TOKEN, mode 600)",
      "probe": {
        "type": "cloudflare_token_file",
        "path": "~/.config/carr/tokens.env",
        "token_key": "CLOUDFLARE_API_TOKEN",
        "url": "https://api.cloudflare.com/client/v4/user/tokens/verify",
        "timeout_s": 15
      },
      "replacement_plan": "Already the long-lived scoped replacement for the retired interactive `wrangler login` OAuth grant. If this probe fails, mint a fresh scoped token in the Cloudflare dashboard (Workers Scripts:Edit + Account Settings:Read on the CARR account only), paste it into ~/.config/carr/tokens.env as CLOUDFLARE_API_TOKEN=<value>, and `chmod 600` the file. If the finding is insecure_file_permissions rather than the token itself, `chmod 600 ~/.config/carr/tokens.env` is the whole fix — the token does not need to be rotated for that alone.",
      "doc_pointer": "secrets-inventory.md — Cloudflare: Worker OAuth (IdP) row",
      "expiring_soon_days": 3,
      "expiry": {}
    },
    {
      "name": "claude-cli-oauth-token-studio",
      "display_name": "Claude Code CLI long-lived login (CLAUDE_CODE_OAUTH_TOKEN, Studio)",
      "kind": "bearer_token",
      "machines": ["studio"],
      "location": "~/.config/carr/tokens.env (key CLAUDE_CODE_OAUTH_TOKEN, mode 600)",
      "probe": {
        "type": "claude_cli_token_age",
        "path": "~/.config/carr/tokens.env",
        "token_key": "CLAUDE_CODE_OAUTH_TOKEN",
        "expected_prefix": "sk-ant-oat",
        "expected_length": 108,
        "timeout_s": 30
      },
      "replacement_plan": "Already the long-lived scoped replacement for the interactive Claude Code CLI login, minted by `claude setup-token` (2026-09-24, ~1 year validity). If this probe fails or reports expiring_soon, run `claude setup-token` again, paste the new value into ~/.config/carr/tokens.env as CLAUDE_CODE_OAUTH_TOKEN=<value>, and confirm the file stays mode 600. A rotated token resets this lane's own age tracking on its next run (it re-records today as the mint date the first time it sees the new value's fingerprint).",
      "doc_pointer": "CLAUDE.md — Dell migration trigger section (Claude-only launch machine)",
      "expiring_soon_days": 35,
      "expiry": {}
    },
    {
      "name": "neon-api-key",
      "display_name": "NEON_API_KEY",
      "kind": "api_key",
      "machines": ["studio"],
      "location": "~/.config/carr/db.env",
      "probe": {
        "type": "neon_api",
        "path": "~/.config/carr/db.env",
        "token_key": "NEON_API_KEY",
        "url": "https://console.neon.tech/api/v2/projects",
        "timeout_s": 15
      },
      "replacement_plan": "Already a long-lived scoped key (control-plane read, per secrets-inventory.md). Rotate it in the Neon console if this probe fails and re-paste into db.env by hand; no agent holds the value.",
      "doc_pointer": "secrets-inventory.md — Neon API key row",
      "expiring_soon_days": 3,
      "expiry": {}
    },
    {
      "name": "carr-mcp-probe-token",
      "display_name": "CARR_MCP_PROBE_TOKEN",
      "kind": "bearer_token",
      "machines": ["studio"],
      "location": "~/.config/carr/mcp-tokens.env",
      "probe": {
        "type": "worker_bearer_health",
        "path": "~/.config/carr/mcp-tokens.env",
        "token_key": "CARR_MCP_PROBE_TOKEN",
        "token_env": "CARR_MCP_PROBE_TOKEN",
        "url_env": "CARR_MCP_URL",
        "url": "https://api.practicecre.com/mcp",
        "timeout_s": 15
      },
      "replacement_plan": "Server-issued bearer, already long-lived and scoped to the locked 'probe' profile (mcp-server/src/mcp.js). If it fails, Joe re-mints it with `wrangler secret put PROBE_TOKENS` per the runbook in mcp-server/smoke-reads.sh's header and re-pastes the matching value into mcp-tokens.env; no agent ever holds it.",
      "doc_pointer": "secrets-inventory.md — PROBE_TOKENS row",
      "expiring_soon_days": 3,
      "expiry": {}
    },
    {
      "name": "carr-mcp-local-token",
      "display_name": "CARR_MCP_LOCAL_TOKEN",
      "kind": "bearer_token",
      "machines": ["studio", "macbook"],
      "location": "~/.config/carr/mcp-tokens.env (per-machine value)",
      "probe": {
        "type": "worker_bearer_health",
        "path": "~/.config/carr/mcp-tokens.env",
        "token_key": "CARR_MCP_LOCAL_TOKEN",
        "token_env": "CARR_MCP_LOCAL_TOKEN",
        "url_env": "CARR_MCP_URL",
        "url": "https://api.practicecre.com/mcp",
        "timeout_s": 15
      },
      "replacement_plan": "Server-issued bearer, already long-lived, one per machine (joe-local / dell-local). If it fails, Joe re-mints via `wrangler secret put LOCAL_TOKENS` per pipelines/provision-local-client.sql and re-pastes the matching value on the affected machine; no agent ever holds it.",
      "doc_pointer": "secrets-inventory.md — LOCAL_TOKENS row",
      "expiring_soon_days": 3,
      "expiry": {}
    },
    {
      "name": "gh-cli-auth",
      "display_name": "gh CLI OAuth (GitHub)",
      "kind": "oauth_local",
      "machines": ["studio"],
      "location": "OS keyring (gh's own credential store)",
      "probe": {
        "type": "shell_exit_status",
        "command": ["gh", "auth", "status"],
        "timeout_s": 15
      },
      "replacement_plan": "Replace with a fine-grained personal access token (repo-scoped to jbookout/carr-system, no org-wide grant), stored the same way as any other CARR token and rotated on the date Joe sets below once he creates it.",
      "doc_pointer": "secrets-inventory.md — GitHub PAT row",
      "expiring_soon_days": 5,
      "expiry": {
        "configured_rotation_date": null,
        "_note": "Joe sets configured_rotation_date (YYYY-MM-DD) here once the fine-grained PAT exists and carries an expiry; null means no configured window and the probe result stands on its own."
      }
    },
    {
      "name": "claude-cli-studio",
      "display_name": "Claude Code CLI login (Studio)",
      "kind": "cli_login",
      "machines": ["studio"],
      "location": "macOS Keychain, service \"Claude Code-credentials\"",
      "probe": {
        "type": "shell_exit_status",
        "command": ["security", "find-generic-password", "-s", "Claude Code-credentials"],
        "timeout_s": 10
      },
      "replacement_plan": "Replace interactive login with `claude setup-token` (a long-lived scoped token) so an unattended run never hits an interactive auth prompt.",
      "doc_pointer": "CLAUDE.md — Dell migration trigger section (Claude-only launch machine)",
      "expiring_soon_days": null,
      "expiry": {}
    },
    {
      "name": "claude-cli-macbook",
      "display_name": "Claude Code CLI login (MacBook)",
      "kind": "cli_login",
      "machines": ["macbook"],
      "location": "macOS Keychain, service \"Claude Code-credentials\"",
      "probe": {
        "type": "shell_exit_status",
        "command": ["security", "find-generic-password", "-s", "Claude Code-credentials"],
        "timeout_s": 10
      },
      "replacement_plan": "Replace interactive login with `claude setup-token` (a long-lived scoped token) so an unattended run never hits an interactive auth prompt. Only meaningful when this lane runs ON the MacBook — probed from the Studio it can only ever read the Studio's own keychain entry of the same service name.",
      "doc_pointer": "CLAUDE.md — Dell migration trigger section (Claude-only launch machine)",
      "expiring_soon_days": null,
      "expiry": {}
    },
    {
      "name": "codex-cli-auth-studio",
      "display_name": "Codex CLI auth (Studio)",
      "kind": "file_credential",
      "machines": ["studio"],
      "location": "~/.codex/auth.json",
      "probe": {
        "type": "file_presence_age",
        "path": "~/.codex/auth.json",
        "warn_after_days": 60
      },
      "replacement_plan": "The complete absence of ~/.codex is an expected supported state on a Claude-only launch machine (CLAUDE.md, Dell migration trigger) — this row reports `unknown` there, never `failed`. Where Codex is in use, replace with whatever long-lived token Codex's own CLI issues in place of interactive ChatGPT-token auth, once one exists.",
      "doc_pointer": "bin/council-lib.sh — ~/.codex/auth.json note",
      "expiring_soon_days": null,
      "expiry": {}
    },
    {
      "name": "codex-cli-auth-macbook",
      "display_name": "Codex CLI auth (MacBook)",
      "kind": "file_credential",
      "machines": ["macbook"],
      "location": "~/.codex/auth.json",
      "probe": {
        "type": "file_presence_age",
        "path": "~/.codex/auth.json",
        "warn_after_days": 60
      },
      "replacement_plan": "Same as the Studio row. Only meaningful when this lane runs ON the MacBook — probed from the Studio it reads the Studio's own filesystem, not the MacBook's.",
      "doc_pointer": "bin/council-lib.sh — ~/.codex/auth.json note",
      "expiring_soon_days": null,
      "expiry": {}
    },
    {
      "name": "google-oauth-worker-client",
      "display_name": "Google OAuth client (GOOGLE_CLIENT_ID/SECRET, Worker sign-in)",
      "kind": "oauth_client",
      "machines": ["worker"],
      "location": "Worker secret (wrangler); OAuth client in Google Cloud project carr-record-layer",
      "probe": {
        "type": "config_only"
      },
      "replacement_plan": "No local probe is possible — Joe holds this credential and no agent ever has (secrets-inventory.md). If the Google Cloud consent screen is still in 'testing' publishing status, Google expires the refresh token after 7 days; the fix is Joe publishing the consent screen (removes the 7-day cap) or re-consenting within the window. Set `expiry.google_testing_mode` true and `expiry.consent_screen_last_verified` to today whenever Joe re-verifies, so this row can compute the live window.",
      "doc_pointer": "secrets-inventory.md — GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET rows",
      "expiring_soon_days": 2,
      "expiry": {
        "google_testing_mode": null,
        "consent_screen_last_verified": null,
        "_note": "Joe sets google_testing_mode (true/false) and consent_screen_last_verified (YYYY-MM-DD, the date he last confirmed the consent screen state) by hand. Both null means 'not yet configured' and the row reports unknown rather than guessing."
      }
    }
  ]
}
