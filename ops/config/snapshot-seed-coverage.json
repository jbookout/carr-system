{
  "_doc": [
    "Every table an applied migration inserts into at migration time, classified.",
    "ops/snapshot_seed_coverage.py refuses to let bin/schema-snapshot.sh write a file",
    "while any such table is missing from both lists, because that is the shape of the",
    "five traps already patched by hand: the ledger says the migration ran, so its seed",
    "never replays, and a rebuilt database has the table and none of its rows.",
    "",
    "CARRIED means the snapshot must contain its rows and the check asserts they are there.",
    "Adding to this list is NOT this file's decision to make on its own: the rule in",
    "bin/schema-snapshot.sh still governs it — a table qualifies because someone read its",
    "rows and can say what is in them, never because a migration seeded it. Adding a table",
    "here without also adding the block that emits it makes the snapshot refuse, which is",
    "the intended order: decide, then carry.",
    "",
    "EXCLUDED means a rebuild is correct without those rows. Excluding is the safe",
    "direction - the cost of a wrong exclusion is the status quo, while the cost of a wrong",
    "carry is client or runtime data in a tracked file. Each entry says why.",
    "",
    "CARRIED_SUBSET means the table is carried in part by a scoped render because it holds",
    "two kinds of row. It must be present, and the reason states the scope.",
    "",
    "CARRIED_AFTER_APPLY is for a reviewed bounded seed whose source migration is",
    "not yet in the snapshot ledger. It must be absent while pending and becomes",
    "row-required automatically when the migration enters the ledger."
  ],
  "carried_after_apply": {
    "ops.completion_policy": "the single versioned completion-default policy seeded by migration 0431; it contains only tenant metadata, the closed required-dimension list, freshness, precedence, effective time, and its digest. Without it the completion projection has no policy and emits no subjects. Carried by an exact source-verified block in bin/schema-snapshot.sh; no subjects, observations, relations, dispositions, or receipts are carried"
  },
  "carried": {
    "activity_kind": "closed vocabulary, foreign-key target; bounded vocabulary pg_dump",
    "actor": "internal actor identities (joe, dell, system, automation) that events point at",
    "agent_profile": "A MIGRATION-SEEDED REGISTRY WITH NO RUNTIME WRITER, and the earlier reason here was wrong on the facts. 0284 seeds the named profiles; nothing inserts the table at runtime — mcp-server/src/agent-profiles.js only UPDATEs, and mcp-server/src/bot-brief.js THROWS profile_not_found when a profile is missing, its own hint saying 'new profiles are a migration'. The previous entry claimed that file tolerates an empty set, which it does not. A rebuild without these rows makes the bot brief fail for every named profile. Read before carrying: profile_key, display_name, a charter of role sentences, status — no client, party, deal or event content. Loud rather than silent, unlike the doctrine registry, but the same wrong-premise shape",
    "client_status": "closed vocabulary; client.status is a foreign key to it",
    "client_type": "closed vocabulary, foreign-key target",
    "contact_state": "closed vocabulary, foreign-key target",
    "deal_lane": "closed vocabulary, foreign-key target",
    "deal_phase": "the eight closed deal phases; foreign-key target",
    "deal_type_ref": "closed vocabulary, foreign-key target",
    "diagnostic_route": "closed vocabulary of stagnation routes",
    "doctrine_edge_type": "closed vocabulary of doctrine graph edges",
    "doctrine_gate_check": "THE DOCTRINE VALIDATION REGISTRY, not gate results — those are doctrine_gate_finding and doctrine_gate_run. Its own comment in 0075 states the contract: 'A NEW GATE IS A FUNCTION AND A ROW.' 0075 seeds 11 rows, all severity=block and enabled, and nothing writes the table at runtime. Its only consumer is runGates() in mcp-server/src/doctrine.js, which selects the enabled checks — so a database rebuilt without them loads an empty registry, produces no findings, and lets EVERY doctrine write pass with no gate run at all. Read before carrying: check_key, severity, impl_key and a one-line description, no client, party, deal or event content. An earlier revision of this file excluded it as 'runtime evidence', which was simply wrong, and that wrong exclusion was the seventh instance of the trap this check exists to catch",
    "doctrine_meta": "singleton bootstrap row appended as deterministic SQL, not vocabulary",
    "doctrine_review_policy": "closed vocabulary of review policies",
    "lead_lane": "closed vocabulary, foreign-key target",
    "lead_stage": "closed vocabulary, foreign-key target",
    "loop_domain": "closed vocabulary, foreign-key target",
    "negotiation_claim_type": "closed vocabulary, foreign-key target",
    "ops.enforcement_control_catalog": "reviewed controls rendered from the repository's own declarations; without them 0228 cannot validate pinned rules",
    "ops.execution_environment_conformance": "governed execution seeds appended as deterministic SQL (0309)",
    "ops.execution_environment_provider": "governed execution seeds appended as deterministic SQL (0309)",
    "ops.execution_environment_provider_event": "governed execution seeds appended as deterministic SQL (0309)",
    "ops.guidance_registry": "the registry's own singleton header row; guidance-registry-db-gate fails without it",
    "ops.job_definition": "the two fixed internal job contracts (renewal-radar-source-daily, governed execution); never widened to arbitrary job rows",
    "ops.rule_delivery_activation_target": "the exact ledger-appropriate reviewed cutover targets; without them a rebuild has no cutover target set",
    "ops.rule_delivery_policy": "bounded singleton delivery policy; without it a rebuild comes up mode:null",
    "ops.siep_component_alias": "the 25 SCAC/MPE component aliases. The same db-gate asserts the exact alias mapping against its reviewed manifest; carried by the sealed-restore block, digest-verified at load",
    "ops.siep_package_contract": "the 40 reviewed SIEP package contracts. ops/siep-program-local-pg-gate.py asserts the exact package set, so a rebuild without them fails that db-gate; carried by the sealed-restore block, digest-verified at load",
    "ops.siep_program_dependency": "the 88-edge SIEP dependency DAG. The same db-gate asserts all 88 edges exactly and fails on any drift; carried by the sealed-restore block, digest-verified at load",
    "participant_role": "closed vocabulary, foreign-key target",
    "party_link_kind": "closed vocabulary, foreign-key target",
    "retrieval_proposal": "0135's ten pending seed proposals; situation-retrieval-db-gate expects exactly ten",
    "retrieval_ranking_policy": "the two versioned ranking policies; the active row carries the golden_suite_digest the assertion looks up",
    "submarket_condition": "closed vocabulary, foreign-key target",
    "vendor_category": "closed vocabulary, foreign-key target",
    "vendor_disposition": "closed vocabulary, foreign-key target",
    "vendor_relationship_level": "closed vocabulary, foreign-key target"
  },
  "carried_subset": {
    "ops.work_request": "CARRIED IN PART, SIEP program rows only: the ops.work_request rows that ops.siep_package_contract.work_request_id points at, restored by the SIEP manifest block in bin/schema-snapshot.sh with the insert guards and seals disabled for the restore and the manifest digest asserted afterwards. Those rows are internal program configuration created by 0324's own literal package list: requester and owner 'joe', no client, party, deal, event, doctrine or credential reference, and their only outbound foreign key is public.actor, which this snapshot already carries. NEVER the sourced captures (WR-000001 onward) - those are operational history."
  },
  "excluded": {
    "agent_profile_assignment": "A RULING LEDGER, append-only, and the earlier reason here was wrong on the facts. It said 0284's immutability probe was the only migration-time insert; 0300 also seeds EIGHT permanent rows - the Hermes operational roster, with fixed idempotency keys, ruling_basis 'human', and its own assertion that all eight exist. So this table IS seeded, and the exclusion has to stand on what the rows are FOR rather than on a claim that they do not exist. Nothing selects it: mcp-server/src/agent-profiles.js only INSERTs (the assign-profile verb), and no gate, verb or view reads it back. A rebuild without these rows therefore loses the recorded history of who assigned which model to which desk, and breaks nothing at runtime - unlike agent_profile, which is carried because bot-brief THROWS profile_not_found without it. Read before excluding: profile_id, model, desk, status, who ruled and on what basis. Excluded as history, not as absence.",
    "campaign": "marketing campaign records — business data",
    "client": "client records — the business data this snapshot exists to keep out of git",
    "code_subject": "code review subjects — runtime history",
    "deal_participant": "deal participation records — business data",
    "deal_reattach_log": "a record of deal reattachments performed on production — business history",
    "defect": "the defect log — runtime history",
    "deprecation": "the deprecation register — runtime history",
    "event": "the event log — business data",
    "marketing_subject": "marketing subjects pointing at clients and deals — business data",
    "media_recommendation": "media recommendations — business content, not configuration",
    "national_account_owner": "national account ownership records — business data",
    "negotiation_claim": "counterparty negotiation observations — business data",
    "ops.capability_agent_session": "capability session records — runtime evidence",
    "ops.capability_verification": "capability verification receipts — runtime evidence",
    "ops.deployment": "deployment history — runtime records",
    "ops.guidance_intake": "guidance intake records — runtime history",
    "ops.incident": "incident records — runtime history",
    "ops.incident_fact": "per-incident evidence — runtime history",
    "ops.job_receipt": "per-run job receipts — runtime history",
    "ops.legacy_prior_staging_readback_allowlist": "a one-time legacy staging-replay allowlist; a fresh database has no legacy replay to allow",
    "ops.release": "release history — runtime records",
    "ops.rule_admission": "per-rule admission history",
    "ops.rule_approval_lifecycle_anchor": "per-rule lifecycle anchors — rule history",
    "ops.rule_control_binding": "per-rule history, not bounded internal control configuration; bin/schema-snapshot.sh names this table by name as one never to add",
    "ops.rule_enforcement_point": "per-rule enforcement records",
    "ops.rule_pack": "0291's only migration-time insert is a probe that asserts its own row does NOT survive; the table holds no migration-seeded rows. Runtime pack rows are outside this check's scope, which is migration-seeded rows only",
    "ops.run": "job run history — runtime records",
    "ops.service": "observability service inventory — runtime state, rebuilt by the environment it describes",
    "ops.service_environment": "observability environment rows — runtime state",
    "ops.settings_change": "settings change receipts — runtime history",
    "ops.tour": "tour operations records (0318) — business data",
    "ops.tour_conflict_resolution_receipt": "per-tour conflict resolution receipts — business data",
    "ops.tour_fact_conflict": "per-tour fact conflicts — business data",
    "ops.tour_fact_conflict_participant": "per-tour conflict participants — business data",
    "ops.tour_field_assertion": "per-tour field assertions — business data",
    "ops.tour_property": "tour property records — business data",
    "ops.tour_property_membership": "tour property membership — business data",
    "ops.tour_public_projection": "published tour projections — business data",
    "ops.tour_public_projection_fact": "published tour projection facts — business data",
    "ops.tour_public_projection_seal_receipt": "tour projection seal receipts — business data",
    "ops.tour_rights_receipt": "tour rights receipts — business data",
    "ops.tour_share_grant": "tour share grants — business data",
    "ops.tour_source_evidence": "per-tour source evidence — business data",
    "ops.work_request_execution_environment_binding": "per-request environment bindings — operational history",
    "org_merge_log": "a record of org identity merges performed on production — business history",
    "party": "party records — business data",
    "party_link": "party relationship records — business data",
    "prospect_pool": "prospect records — business data",
    "record_flag": "flags raised against business rows — per-row history",
    "system_config": "operational settings, not vocabulary, and the kind of table where a value that should never be committed can appear; staging sets its own",
    "tool_call": "the verb call ledger — runtime history"
  },
  "carried_subset_must_not_contain": {
    "ops.work_request": {
      "sourced captures": "\"ref\":\\s*\"WR-\\d{6,}\""
    }
  }
}
