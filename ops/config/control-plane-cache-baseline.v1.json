{
  "schema_version": 1,
  "kind": "baseline_only",
  "measure": "cognition_cache_events",
  "target_evaluation": "refused_until_a_separate_approved_target_contract_references_this_baseline_digest",
  "coverage": {
    "population": "every cache-eligible cognition attempt in the supplied immutable ledger window",
    "required": "every expected attempt has exactly one cache read observation and every observation reconciles to that population"
  },
  "event_kinds": ["hit", "miss", "store", "invalidate", "invalidated", "expired"],
  "authority": "measurement only; this contract sets no SLO, reliability target, acceptance, or canonical-write authority"
}
