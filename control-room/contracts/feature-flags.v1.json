{
  "contract": "carr-control-room-phase0-feature-flags",
  "version": "1.0.0",
  "status": "phase0_frozen",
  "flags": [
    {
      "key": "control_room_readonly_v1",
      "default": false,
      "audience": "Joe internal prototype",
      "server_enforced": true,
      "purpose": "Expose the fixture-backed read-only Control Room shell and prototype routes.",
      "rollback": "Disable the flag and return a static safe not-available page.",
      "mutation_capability": false
    }
  ],
  "telemetry": ["route and view load", "auth audience refusal", "API outcome and latency", "freshness state and source age", "refusal class", "event replay gap", "prototype flow completion"],
  "telemetry_prohibitions": ["secret values", "client payloads", "raw logs", "raw prompts", "authorization material"],
  "migration": {"phase0": "none", "future": "separate forward-only checksummed ops schema, rehearsed on a Neon branch with least privilege and a named forward fix"},
  "support": ["source", "observed or verified time", "freshness", "correlation ID", "safe next action", "runbook or reference"],
  "rollback": ["disable feature flag", "serve static safe page", "retain prior versioned fixture assets", "no database rollback because Phase 0 creates no production schema or data"]
}
