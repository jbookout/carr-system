# carr-system Python dependencies (orchestrator-lane corrective, 2026-07-25).
# Install: python3 -m pip install -r requirements.txt
# Everything else in the repo is stdlib by design (repo convention).
openpyxl>=3.1
Pillow>=10.0   # used by the write-content card crop step + image handling in space-search
psycopg[binary]>=3.2   # record layer: tools/migrate.py + (build day) the MCP server's DB path

# UNDECLARED UNTIL 2026-08-13, found by ops/ci-dep-check.py on its first run. The
# repo convention line above ("everything else is stdlib by design") had quietly
# stopped being true: these four were installed by hand on Joe's Mac and written
# down nowhere, so a clean install from this file produced a machine that looked
# correct and broke on first use. The first two are the worst of it — they are the
# local half of the prepare-document verb, the code that writes and scrubs the
# actual Word LOIs and letters that go to a listing agent.
python-docx>=1.2   # fill-engine/fill_document.py + pipelines/prepare_document.py: writes the .docx
lxml>=6.0          # fill-engine/fill_document.py scrub_docx's XML pass; imported directly, so its
                   # own line rather than relying on arriving transitively through python-docx
numpy>=2.0         # tools/doc-voice/render-doc.py terminal-slope scoring, on the ROOT venv
pytest>=9.0        # tools/test-doctrine-mirror.py; a test dependency is still a dependency

# UNDECLARED WHEN IT ARRIVED, and found by importing it rather than by any gate.
# pipelines/check-brand.py reads a finished PDF back and checks every page
# against the brand rules before a packet is delivered. It imports fitz, which
# was this package, and nothing declared it — so the check standing between a
# client and a mis-rendered tour packet died on ImportError the first time
# anyone ran it. That is the failure the four lines above already record from
# 2026-08-13: installed by hand on one Mac, written down nowhere, and a clean
# install looks correct until first use.
#
# ci-dep-check.py cannot catch this class of gap. It proves declared packages
# are pinned and that the lock matches this file; it never reads the source for
# imports, so an undeclared one stays invisible to it.
pymupdf>=1.28      # pipelines/check-brand.py, the pre-delivery brand reader

# SYSTEM (non-pip) dependencies, documented here because nowhere else was:
#   libreoffice  — shared/fill-engine/fill_deal_template.py renders via `libreoffice --headless`
#   node         — workflows/*.workflow.js syntax checks (tools/smoke.sh); the Workflow
#                  runtime itself is provided by Claude Code, not this repo
#   git          — manifest/baseline discipline assumes a git checkout
# Development/nightly static checking only.  The declared type-check scope
# requires mypy 2.3+, but Dell's older runtime does not run that dev tripwire;
# the marker keeps its migration/runtime dependency install compatible.
mypy>=2.3; python_version >= "3.10"

# EventKit binding for tools/calendar-attendee-probe.py and
# tools/calendar-touch-matcher.py, which read the LOCAL Apple Calendar store.
# UNDECLARED UNTIL 2026-08-13, and the gap was worse than a missing package: with
# it absent the probe dies on `import objc` before it ever asks for permission, so
# the moment Joe grants Calendar access the tool would still have failed and looked
# exactly like the grant not working. Declared here so the dependency and the
# permission are two separate, separately-diagnosable answers.
#
# macOS only by construction — EventKit does not exist elsewhere — and the marker
# keeps Dell's install working rather than failing on a framework his machine has.
pyobjc-framework-EventKit>=10.0; sys_platform == "darwin"
