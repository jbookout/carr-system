# R13 — Frontier forensics: why the production lane parks and what any fix must respect (primary evidence, this session, 2026-09-01)

Measured on production (read-only taps, receipts in session transcript):
- Applied ledger tops at 0453 (0451 applied after Joe's receipted default-privilege pre-step; 0452-0453 followed). The apply run stops at 0454 with: "SCAC database catalog category secdef_execute drifted: count 600, digest sha256:54c0..." — production's live census (600 security-definer entries among connected runtime roles) against the migration's pinned baseline (347), computed on a from-scratch disposable database.
- The registry migrations' own text (0471, regenerated inspection): "v1-v9 remain source-only until Joe approves Production application"; write modes for historical seals are refused ("sealed historical evidence; create a forward successor instead").
- The runner (tools/migrate.py) already carries ONE reviewed conditional mechanism: DATA_DEPENDENT_MIGRATIONS - a per-file precondition query that must return EMPTY for the file to be inert-skipped; explicitly "NOT a skip-on-failure door"; each entry names one file, one precondition, and a checkable reason. It also carries HISTORICAL_TRANSACTION_CONTROL_ARTIFACTS and REVIEWED_TRANSACTION_CONTROL_ARTIFACTS: digest-pinned allowlists where "an unreviewed filename or one-byte change refuses before any SQL executes." These are the design grammar a production-gated register must extend.
- Cascade law (measured the hard way): tools/migrate.py is an inventoried mutation source (row in the SCAC inventory, 804 source entries). ANY byte change forces: runtime registry regen (--write-runtime-v10) -> forward migration regen (--write-source-merge-forward-registry-migration, 1.7MB re-render exposure if the tree drifted) -> ops/config/scac-registry-full-entry-set-seals.json v10 reseal (hand-maintained, validated by ops/schema-snapshot-registry-seed-selftest.py) -> bin/schema-snapshot.sh guard (reads the seals file + runtime constants + the live ops.scac_mutation_catalog_v10_current() census) -> committed db/schema.sql. Five layers, one deliberate reviewed change.
- The catalog census function pins secdef EXECUTE (347), relation DML (285), column DML (12), and role/membership/ownership rows (52) with digests; SELECT grants and default ACLs are censused NOWHERE (verified by probing each block) - which is why the backup-grant restore was seal-safe.
- Interim state: rule-binding row (0472 content) and backup-grant restore (0473 content) landed on production via Joe-approved receipted break-glass; both migrations' file replays are proven no-ops (on-conflict-do-nothing; idempotent grants). Any register design must leave replay-over-breakglass-rows harmless - it already is.
- Bootstrap path a fix must not break: disposable builds load committed db/schema.sql then apply pending migrations; the local lane's verify step compares the disposable DB against runtime-file constants and the seals config. A register that skips files on production must NOT skip them on disposables (they apply cleanly there), so the gate condition must be a live-database probe (has the integrity program activated here? does the census baseline hold here?) - not an environment name.
